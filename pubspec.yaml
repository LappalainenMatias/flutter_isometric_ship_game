name: anki
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  intl: ^0.17.0
  flutter_shaders: ^0.1.0
  flutter_joystick: ^0.0.2
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  matrix2d: ^1.0.4
  open_simplex_noise: ^2.3.1
  isolated_worker: ^0.1.1
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  test: ^1.20.0

flutter:
  uses-material-design: true
  shaders:
    - shaders/regtanglewater.frag
